# Flask Configuration
FLASK_APP=app.py
FLASK_ENV=development
SECRET_KEY=your-secret-key-here

# Database Configuration (Internal)
DATABASE_URL=postgresql://crumple:crumple@postgres:5432/crumple

# RabbitMQ Configuration (Internal)
RABBITMQ_URL=amqp://guest:guest@rabbitmq:5672

# Server Configuration
HOST=0.0.0.0
PORT=3000  # Frontend port

# Network Configuration
# The web service is accessible externally on port 3000
# All other services communicate internally
# No need to expose ports for:
# - PostgreSQL (5432)
# - RabbitMQ (5672, 15672)

# Optional: Custom server name for reverse proxy setups
# SERVER_NAME=your-domain.com
# PREFERRED_URL_SCHEME=https

# Note: Netbox configuration is managed through the web interface
# Configure your Netbox connection at http://localhost:3000/settings
